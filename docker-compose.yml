services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      # Single persistent volume — db, uploads, and backups all live under here, matching
      # INVENTORY_DATA_DIR below. Survives container rebuilds and updates.
      - ./appdata:/data
    environment:
      - PYTHONUNBUFFERED=1
      - INVENTORY_DATA_DIR=/data
